<form method="post" action="{{ path('app_gestion_obligation_obligation_delete', {'id': obligation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ obligation.id) }}">
    <button class="btn">Delete</button>
</form>
