{% extends 'base.html.twig' %}

{% block title %}TypeDocContrat{% endblock %}

{% block body %}
    <h1>TypeDocContrat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_doc_contrat.id }}</td>
            </tr>
            <tr>
                <th>Path</th>
                <td>{{ type_doc_contrat.path }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ type_doc_contrat.createdAt ? type_doc_contrat.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ type_doc_contrat.updatedAt ? type_doc_contrat.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>OriginalName</th>
                <td>{{ type_doc_contrat.originalName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gestion_base_documentaire_type_doc_contrat_index') }}">back to list</a>

    <a href="{{ path('app_gestion_base_documentaire_type_doc_contrat_edit', {'id': type_doc_contrat.id}) }}">edit</a>

    {{ include('gestion_base_documentaire/type_doc_contrat/_delete_form.html.twig') }}
{% endblock %}
